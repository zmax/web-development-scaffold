{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // --- 環境與模組設定 (Vite + React) ---
    "lib": ["DOM", "DOM.Iterable", "ESNext"],
    "jsx": "react-jsx",
    "module": "ESNext",
    "moduleResolution": "bundler",
    // --- 輸出設定 ---
    // 對於使用 Vite 的應用程式，tsc 只用於型別檢查，不產生輸出檔案。
    "noEmit": true,
    // --- 內部路徑別名 ---
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src"],
  "references": [
    // 定義對共享套件的建構依賴
    {
      "path": "../../packages/ui"
    },
    {
      "path": "../../packages/utils"
    },
    {
      "path": "../../packages/types"
    }
  ]
}
