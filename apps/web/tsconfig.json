{
  // This file is intentionally left empty.
  // It's used by `tsc --build` to discover the real tsconfig files.
  "files": [],
  "references": [
    { "path": "./tsconfig.dev.json" },
    { "path": "./tsconfig.build.json" }
  ]
}
